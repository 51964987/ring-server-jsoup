<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ring.server.jsoup.mvc.dao.page.PageDetailMapper">
	<!-- 增加 -->
	<insert id="add" parameterType="ring.server.jsoup.mvc.model.page.PageDetail">
		INSERT INTO page_detail (id,title,content,images,magnet,torrent,url,source,sort)
		VALUES(#{id},#{title},#{content},#{images},#{magnet},#{torrent},#{url},#{source},#{sort})
	</insert>

	<!-- 映射 
	<resultMap type="ringbackstage.web.model.log.AdminLog" id="adminLogResultMap">
		<result property="id" column="id" />
		<result property="logType" column="log_type" />
		<result property="uri" column="uri" />
		<result property="reqMethod" column="req_method" />
		<result property="reqParams" column="req_params" />
		<result property="code" column="code" />
		<result property="message" column="message" />
		<result property="errorMsg" column="error_msg" />
		<result property="createIp" column="create_ip" />
		<result property="createDate" column="create_date" />
		<result property="createBy" column="create_by" />
	</resultMap>
	-->
</mapper>